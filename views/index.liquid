{% render 'views/head.liquid', title: 'Squad page' %}
{% comment %} Miel {% endcomment %}

<section class="carrousel">

<div class="hidden">
  <div>
    {% for person in ninetySeventyOne %}
      <article>
        <a>  <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in ninetySeventyThree %}
      <article>
      <a> <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in ninetyEightyFour %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in ninetyNinety %}
      <article>  
        <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

<div class="hidden">
  <div>
    {% for person in ninetyNinetyFour %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in ninetyNinetyNine %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in twoThousands %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
</div>

<div class="hidden">
  <div>
    {% for person in twoThousandOne %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandTwo %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandThree %}
      <article>
        <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandFour %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandFive %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}" style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandSix %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}"style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>

  <div class="hidden">
  <div>
    {% for person in twoThousandSeven %}
      <article>
       <img src="{{ person.avatar }}" width="150px" href="/student/{{ person.id }}"style="border: 5px {{ person.fav_color }} solid"></a>
      <p> {{ person.name }} </p>
      </article>
    {% endfor %}
  </div>
  </div>
</section>

<div class="year">
  {% comment %} hier komt de waarde die ook zichtbaar is op de slider {% endcomment %}
    <h2 class="yearof">1970</h2>
</div>

{% comment %} Kate {% endcomment %}
<div>
  <img src="{{ person.avatar }}" width="150px">
  <p>{{ person.name }}</p>
  <p>{{ person.bio }}</p>
</div>

<h2>Comments</h2>

{% for message in messages %}
<p>{{ message.from }}: {{ message.text }} @{{ message.created | date: '%d-%m-%Y %H:%M' }}</p>
{% else %}
<p>Er zijn nog geen berichten voor dit team.</p>
{% endfor %}

 <section>
<h2>Voeg een bericht toe</h2>
  <form method="post" action="/">
    <label>
      Naam:
      <input type="text" name="from" required>
    </label>
    <label>
      Bericht:
      <textarea name="text" required></textarea>
    </label>
    <button type="submit">Verstuur</button>
  </form>
</section>

{% comment %} Kyan {% endcomment %}
  
  <section class="slidecontainer">
    <input class="slider" type="range" min="1970" max="2010" value="1970" class="slider" id="myRange">
  </section>

{% render 'views/end.liquid', squads: squads %}